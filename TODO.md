# TODOs

- Add CLI authentication with login and session token.
- Implement token/session management with expiration and refresh.
- Validate API key before running commands.
- Handle server errors gracefully with user-friendly messages.
- Add logging for authentication and failed attempts.
- Consider OAuth or other secure authentication for commercial use.
- Secure endpoints with SAML/SSO/OAuth (zero trust).
- Implement role-based access control (RBAC) for sensitive actions.
- Add UDP tunnel support. (Without relying on TCP encapsulation)
- Support configuration file for defaults (API key, subdomain).
- Improve network retry mechanism.
- Add unit and integration tests for CLI.
- Enhance documentation with examples and edge cases.

<!-- - HTTP/2 + HTTP/3 support. -->
<!-- - Streaming large file uploads/downloads without buffering entirely in memory. -->
<!-- - Thread or process pools for high-throughput TCP forwarding -->
<!-- - Per-connection ACLs -->